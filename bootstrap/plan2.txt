data.aws_caller_identity.current: Reading...
data.aws_caller_identity.current: Read complete after 0s [id=163876584821]
aws_s3_bucket.state: Refreshing state... [id=tg-agent-tfstate-81b4d8bc]
aws_s3_bucket_versioning.state: Refreshing state... [id=tg-agent-tfstate-81b4d8bc]
aws_s3_bucket_public_access_block.state: Refreshing state... [id=tg-agent-tfstate-81b4d8bc]
module.github_oidc.data.aws_iam_policy_document.state_access: Reading...
aws_s3_bucket_server_side_encryption_configuration.state: Refreshing state... [id=tg-agent-tfstate-81b4d8bc]
module.github_oidc.data.aws_iam_policy_document.state_access: Read complete after 0s [id=3050028340]
aws_s3_bucket_policy.state_tls_only: Refreshing state... [id=tg-agent-tfstate-81b4d8bc]
aws_s3_bucket_lifecycle_configuration.state: Refreshing state... [id=tg-agent-tfstate-81b4d8bc]

Terraform used the selected providers to generate the following execution
plan. Resource actions are indicated with the following symbols:
  + create
 <= read (data resources)

Terraform will perform the following actions:

  # module.github_oidc.data.aws_iam_policy_document.apply will be read during apply
  # (config refers to values not yet known)
 <= data "aws_iam_policy_document" "apply" {
      + id            = (known after apply)
      + json          = (known after apply)
      + minified_json = (known after apply)

      + statement {
          + actions   = [
              + "apigateway:*",
              + "budgets:*",
              + "cloudwatch:*",
              + "dynamodb:*",
              + "events:*",
              + "lambda:*",
              + "logs:*",
              + "s3:*",
              + "sns:*",
              + "sqs:*",
              + "ssm:*",
            ]
          + effect    = "Allow"
          + resources = [
              + "*",
            ]
          + sid       = "ManageWorkloadResources"
        }
      + statement {
          + actions   = [
              + "iam:Get*",
              + "iam:List*",
              + "iam:SimulatePrincipalPolicy",
            ]
          + effect    = "Allow"
          + resources = [
              + "*",
            ]
          + sid       = "ReadIAM"
        }
      + statement {
          + actions   = [
              + "iam:AttachRolePolicy",
              + "iam:CreateRole",
              + "iam:PutRolePolicy",
            ]
          + effect    = "Allow"
          + resources = [
              + "arn:aws:iam::163876584821:role/tg-agent-*",
            ]
          + sid       = "CreateBoundedRolesOnly"

          + condition {
              + test     = "StringEquals"
              + values   = [
                  + (known after apply),
                ]
              + variable = "iam:PermissionsBoundary"
            }
        }
      + statement {
          + actions   = [
              + "iam:DeleteRole",
              + "iam:DeleteRolePolicy",
              + "iam:DetachRolePolicy",
              + "iam:TagRole",
              + "iam:UntagRole",
              + "iam:UpdateAssumeRolePolicy",
            ]
          + effect    = "Allow"
          + resources = [
              + "arn:aws:iam::163876584821:role/tg-agent-*",
            ]
          + sid       = "ManageOwnRoles"
        }
      + statement {
          + actions   = [
              + "iam:PassRole",
            ]
          + effect    = "Allow"
          + resources = [
              + "arn:aws:iam::163876584821:role/tg-agent-*",
            ]
          + sid       = "PassRoleToLambdaOnly"

          + condition {
              + test     = "StringEquals"
              + values   = [
                  + "lambda.amazonaws.com",
                ]
              + variable = "iam:PassedToService"
            }
        }
    }

  # module.github_oidc.data.aws_iam_policy_document.assume_apply will be read during apply
  # (config refers to values not yet known)
 <= data "aws_iam_policy_document" "assume_apply" {
      + id            = (known after apply)
      + json          = (known after apply)
      + minified_json = (known after apply)

      + statement {
          + actions = [
              + "sts:AssumeRoleWithWebIdentity",
            ]
          + effect  = "Allow"

          + condition {
              + test     = "StringEquals"
              + values   = [
                  + "repo:mishgoldenberg/telegram-agent-aws:ref:refs/heads/main",
                ]
              + variable = "token.actions.githubusercontent.com:sub"
            }
          + condition {
              + test     = "StringEquals"
              + values   = [
                  + "sts.amazonaws.com",
                ]
              + variable = "token.actions.githubusercontent.com:aud"
            }

          + principals {
              + identifiers = [
                  + (known after apply),
                ]
              + type        = "Federated"
            }
        }
    }

  # module.github_oidc.data.aws_iam_policy_document.assume_plan will be read during apply
  # (config refers to values not yet known)
 <= data "aws_iam_policy_document" "assume_plan" {
      + id            = (known after apply)
      + json          = (known after apply)
      + minified_json = (known after apply)

      + statement {
          + actions = [
              + "sts:AssumeRoleWithWebIdentity",
            ]
          + effect  = "Allow"

          + condition {
              + test     = "StringEquals"
              + values   = [
                  + "repo:mishgoldenberg/telegram-agent-aws:pull_request",
                ]
              + variable = "token.actions.githubusercontent.com:sub"
            }
          + condition {
              + test     = "StringEquals"
              + values   = [
                  + "sts.amazonaws.com",
                ]
              + variable = "token.actions.githubusercontent.com:aud"
            }

          + principals {
              + identifiers = [
                  + (known after apply),
                ]
              + type        = "Federated"
            }
        }
    }

  # module.github_oidc.aws_iam_openid_connect_provider.github will be created
  + resource "aws_iam_openid_connect_provider" "github" {
      + arn             = (known after apply)
      + client_id_list  = [
          + "sts.amazonaws.com",
        ]
      + id              = (known after apply)
      + tags            = {
          + "Component" = "github-oidc"
        }
      + tags_all        = {
          + "Component"   = "github-oidc"
          + "Environment" = "shared"
          + "ManagedBy"   = "terraform"
          + "Owner"       = "mishgoldenberg"
          + "Project"     = "tg-agent"
        }
      + thumbprint_list = [
          + "6938fd4d98bab03faadb97b34396831e3780aea1",
        ]
      + url             = "https://token.actions.githubusercontent.com"
    }

  # module.github_oidc.aws_iam_policy.boundary will be created
  + resource "aws_iam_policy" "boundary" {
      + arn              = (known after apply)
      + attachment_count = (known after apply)
      + description      = "Ceiling on every role the CI apply role creates."
      + id               = (known after apply)
      + name             = "tg-agent-gha-boundary"
      + name_prefix      = (known after apply)
      + path             = "/"
      + policy           = jsonencode(
            {
              + Statement = [
                  + {
                      + Action   = [
                          + "logs:*",
                          + "dynamodb:*",
                          + "sqs:*",
                          + "s3:*",
                          + "ssm:GetParameter",
                          + "ssm:GetParameters",
                          + "kms:Decrypt",
                          + "lambda:InvokeFunction",
                          + "cloudwatch:PutMetricData",
                        ]
                      + Effect   = "Allow"
                      + Resource = "*"
                      + Sid      = "AllowWorkloadServices"
                    },
                  + {
                      + Action   = [
                          + "iam:*",
                          + "organizations:*",
                          + "account:*",
                          + "billing:*",
                          + "aws-portal:*",
                          + "sts:AssumeRole",
                        ]
                      + Effect   = "Deny"
                      + Resource = "*"
                      + Sid      = "DenyPrivilegeEscalation"
                    },
                ]
              + Version   = "2012-10-17"
            }
        )
      + policy_id        = (known after apply)
      + tags             = {
          + "Component" = "github-oidc"
        }
      + tags_all         = {
          + "Component"   = "github-oidc"
          + "Environment" = "shared"
          + "ManagedBy"   = "terraform"
          + "Owner"       = "mishgoldenberg"
          + "Project"     = "tg-agent"
        }
    }

  # module.github_oidc.aws_iam_role.apply will be created
  + resource "aws_iam_role" "apply" {
      + arn                   = (known after apply)
      + assume_role_policy    = (known after apply)
      + create_date           = (known after apply)
      + description           = "GitHub Actions: terraform apply on main."
      + force_detach_policies = false
      + id                    = (known after apply)
      + managed_policy_arns   = (known after apply)
      + max_session_duration  = 3600
      + name                  = "tg-agent-gha-apply"
      + name_prefix           = (known after apply)
      + path                  = "/"
      + tags                  = {
          + "Component" = "github-oidc"
        }
      + tags_all              = {
          + "Component"   = "github-oidc"
          + "Environment" = "shared"
          + "ManagedBy"   = "terraform"
          + "Owner"       = "mishgoldenberg"
          + "Project"     = "tg-agent"
        }
      + unique_id             = (known after apply)

      + inline_policy (known after apply)
    }

  # module.github_oidc.aws_iam_role.plan will be created
  + resource "aws_iam_role" "plan" {
      + arn                   = (known after apply)
      + assume_role_policy    = (known after apply)
      + create_date           = (known after apply)
      + description           = "GitHub Actions: terraform plan on pull requests. Read-only."
      + force_detach_policies = false
      + id                    = (known after apply)
      + managed_policy_arns   = (known after apply)
      + max_session_duration  = 3600
      + name                  = "tg-agent-gha-plan"
      + name_prefix           = (known after apply)
      + path                  = "/"
      + tags                  = {
          + "Component" = "github-oidc"
        }
      + tags_all              = {
          + "Component"   = "github-oidc"
          + "Environment" = "shared"
          + "ManagedBy"   = "terraform"
          + "Owner"       = "mishgoldenberg"
          + "Project"     = "tg-agent"
        }
      + unique_id             = (known after apply)

      + inline_policy (known after apply)
    }

  # module.github_oidc.aws_iam_role_policy.apply will be created
  + resource "aws_iam_role_policy" "apply" {
      + id          = (known after apply)
      + name        = "terraform-apply"
      + name_prefix = (known after apply)
      + policy      = (known after apply)
      + role        = (known after apply)
    }

  # module.github_oidc.aws_iam_role_policy.apply_state will be created
  + resource "aws_iam_role_policy" "apply_state" {
      + id          = (known after apply)
      + name        = "terraform-state"
      + name_prefix = (known after apply)
      + policy      = jsonencode(
            {
              + Statement = [
                  + {
                      + Action   = "s3:ListBucket"
                      + Effect   = "Allow"
                      + Resource = "arn:aws:s3:::tg-agent-tfstate-81b4d8bc"
                      + Sid      = "ListStateBucket"
                    },
                  + {
                      + Action   = [
                          + "s3:PutObject",
                          + "s3:GetObject",
                          + "s3:DeleteObject",
                        ]
                      + Effect   = "Allow"
                      + Resource = "arn:aws:s3:::tg-agent-tfstate-81b4d8bc/*"
                      + Sid      = "ReadWriteStateAndLock"
                    },
                ]
              + Version   = "2012-10-17"
            }
        )
      + role        = (known after apply)
    }

  # module.github_oidc.aws_iam_role_policy.plan_state will be created
  + resource "aws_iam_role_policy" "plan_state" {
      + id          = (known after apply)
      + name        = "terraform-state"
      + name_prefix = (known after apply)
      + policy      = jsonencode(
            {
              + Statement = [
                  + {
                      + Action   = "s3:ListBucket"
                      + Effect   = "Allow"
                      + Resource = "arn:aws:s3:::tg-agent-tfstate-81b4d8bc"
                      + Sid      = "ListStateBucket"
                    },
                  + {
                      + Action   = [
                          + "s3:PutObject",
                          + "s3:GetObject",
                          + "s3:DeleteObject",
                        ]
                      + Effect   = "Allow"
                      + Resource = "arn:aws:s3:::tg-agent-tfstate-81b4d8bc/*"
                      + Sid      = "ReadWriteStateAndLock"
                    },
                ]
              + Version   = "2012-10-17"
            }
        )
      + role        = (known after apply)
    }

  # module.github_oidc.aws_iam_role_policy_attachment.plan_readonly will be created
  + resource "aws_iam_role_policy_attachment" "plan_readonly" {
      + id         = (known after apply)
      + policy_arn = "arn:aws:iam::aws:policy/ReadOnlyAccess"
      + role       = "tg-agent-gha-plan"
    }

Plan: 8 to add, 0 to change, 0 to destroy.

Changes to Outputs:
  + gha_apply_role_arn = (known after apply)
  + gha_plan_role_arn  = (known after apply)
  + gha_trusted_subs   = {
      + apply = "repo:mishgoldenberg/telegram-agent-aws:ref:refs/heads/main"
      + plan  = "repo:mishgoldenberg/telegram-agent-aws:pull_request"
    }

─────────────────────────────────────────────────────────────────────────────

Saved the plan to: tfplan

To perform exactly these actions, run the following command to apply:
    terraform apply "tfplan"
Releasing state lock. This may take a few moments...
